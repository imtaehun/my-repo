name: Literals
on: push
jobs:
  print:
    runs-on: ubuntu-22.04
    steps:
      - run: printenv | grep 'LITERAL_'
        env:
          LITERAL_NULL: ${{ null }}
          LITERAL_BOOLEAN: ${{ true }}
          LITERAL_NUMBER: ${{ 1234 }}
          LITERAL_STRING: ${{ 'imtaehun' }}
          LITERAL_OMIT_STRING: java
